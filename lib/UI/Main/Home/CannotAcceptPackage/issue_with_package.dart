import 'package:flutter/material.dart';

class IssueWithPackage extends StatelessWidget {
  const IssueWithPackage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Issue with Package'),
      ),
    );
  }
}
