import 'package:flutter/material.dart';

class BagFull extends StatelessWidget {
  const BagFull({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Bag Full'),
      ),
    );
  }
}
